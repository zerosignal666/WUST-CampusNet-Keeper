# v0.1.0

武科大校园网 Windows 自动认证工具的首个公开版本。

- 依据学校门户 `/api/account/status` 判断当前设备是否在线，仅在确认未认证时尝试登录。
- 自动读取当前网络的 `nasId`，并支持粘贴认证页地址手动提取。
- 核对登录接口业务结果、在线账号和外网恢复情况；认证失败或需要验证码时暂停自动重试。
- 使用 Windows DPAPI 在当前用户下加密保存密码，支持登录 Windows 后自动启动。

下载 `WUST-CampusNet-Keeper.exe` 后双击运行。学校认证接口使用 HTTP；本地密码加密无法保护传输过程。已有使用者在手动下线后验证了恢复登录，自然过期后的长期运行仍待观察。
